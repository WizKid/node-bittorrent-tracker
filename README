The querystring lib trims values and keys and also convert values to integers. You don't want that so I created a patch to node.js to say that you want the raw values. The fork can be found at http://github.com/WizKid/node
